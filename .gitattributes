# Git 属性配置 — 优化二进制文件处理和 Git 操作

# 图片文件标记为二进制，避免 Git 尝试合并
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary

# 使用 Git LFS 管理大型二进制文件 (如需要取消注释)
# *.png filter=lfs diff=lfs merge=lfs -text
# *.webp filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
